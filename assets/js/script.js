// hooks 
questionPageButton = document.getElementById("questions-button");
highscorePageButton = document.getElementById("high-score-button");

// event listeners
questionPageButton.addEventListener("click", e => {
    // remove hidden class from questions div
    // hide rest of pages
});

highscorePageButton.addEventListener("click", e => {
    // remove hidden class from highscore div
    // hide rest of pages
});
